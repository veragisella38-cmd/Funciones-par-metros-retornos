# Funciones-par-metros-retornos
Ejercicio práctico sobre funciones en Python con parámetros y retorno de valores.
# Programación Básica: Funciones con Parámetros y Retorno de Valores

## Estudiante
Kerly Zambrano

## Contexto y Objetivo de la tarea
Aplicar el uso de funciones con parámetros y retorno de valores mediante la creación de un programa sencillo y la explicación de su funcionamiento.

## Actividad desarrollada
El programa resuelve el problema de calcular el área de un rectángulo.  
Se utiliza una función en Python que recibe dos parámetros (base y altura) y devuelve el resultado con la palabra clave `return`.

## Código
```python
def calcular_area(base, altura):
    return base * altura

resultado = calcular_area(10, 5)
print("El área del rectángulo es:", resultado)
